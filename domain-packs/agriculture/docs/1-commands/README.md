# Agriculture Domain — Commands (Section 1)

Admin and operator commands specific to the agriculture domain.

| Command | Description |
|---------|-------------|
| *(no domain-specific commands yet)* | |

Commands that apply to all domains (e.g., `manifest-regenerate`, `integrity-check`) live
in the root [`docs/1-commands/`](../../../../docs/1-commands/).
