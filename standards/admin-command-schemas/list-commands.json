{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lumina://standards/admin-command-schemas/list-commands.json",
  "schema_version": "1.0.0",
  "last_updated": "2026-03-25",
  "title": "list_commands",
  "description": "List all available admin commands with descriptions, HITL status, and required roles. Read-only, HITL-exempt.",
  "type": "object",
  "required": [
    "operation"
  ],
  "additionalProperties": false,
  "properties": {
    "operation": {
      "const": "list_commands"
    },
    "target": {
      "type": "string"
    },
    "params": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "include_details": {
          "type": "boolean",
          "description": "When true (default), include description, HITL status, and required role for each command. When false, return names only."
        }
      }
    }
  }
}
